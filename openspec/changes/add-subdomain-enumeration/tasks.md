## 1. Implementation
- [x] 1.1 初始化最小 FastAPI/uv 工程骨架与 SQLite（WAL）配置。
- [x] 1.2 封装子域枚举执行器（Subfinder 或兼容命令），支持异步运行、日志收集、去重结果。
- [x] 1.3 设计并实现 API（创建任务、查询状态/日志、获取结果），返回标准化错误。
- [x] 1.4 字典管理：API/存储结构支持上传/列出/设置默认字典，枚举任务可选择字典运行。
- [x] 1.5 前端增加子域枚举页面/模块：域名输入、字典选择/上传/设默认、触发任务、轮询状态、展示结果列表（深色主题）。
- [x] 1.6 编写基础测试/脚本覆盖执行器解析、字典管理与 API 正常/失败路径；补充 README/运行说明。
- [x] 1.7 对齐规格文件，完成后勾选任务并验证 `openspec validate`。
- [x] 1.8 丰富前端布局为左侧导航 + 右侧功能区，增加顶部概览信息，沿用 Minimal Dark 配色。
- [ ] 1.9 验证前端与后端交互（手动/最小脚本），并更新前端使用说明。
