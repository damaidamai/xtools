# Change: 添加请求测试功能

## Why
- 需要在工作区提供快捷的 HTTP 请求测试能力，支持重复请求、代理选择与编辑后再发起，方便调试与验证。

## What Changes
- 前端工作区新增「请求测试」页面/入口，支持粘贴 curl 并发起请求。
- 支持配置请求次数（默认 1），依次展示每次返回结果。
- 支持选择已有代理，通过代理发起请求。
- 请求内容可在粘贴后进一步编辑再发起。

## Impact
- 前端导航新增功能入口与页面。
- 后端新增请求解析与转发执行接口（含代理支持与多次执行逻辑）。
