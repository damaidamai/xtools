# Change: 添加代理管理功能

## Why
- 需要在系统内集中管理代理，支持配置并测试连通性，便于后续扫描/枚举任务复用。

## What Changes
- 前端左侧菜单新增“代理管理”入口，位于系统设置分组上方。
- 提供代理的增删改查与启用状态切换，字段包含名称、类型、主机、端口、用户名、密码、备注。
- 支持密码明文存储与回显。
- 提供“测试连通性”操作，校验代理可用性并提示结果。

## Impact
- 影响前端导航与设置页面布局。
- 新增后端代理管理 API、存储模型与连通性测试逻辑。
